# Managed by ReyOS Control Center's Keyboard page ("Custom Shortcuts").
# Do not edit by hand -- this file is regenerated from
# ~/.config/reyos/custom-shortcuts.json every time a shortcut is added or
# removed. Shipped here (even empty) so the autostart entry never fails to
# find a config file on a fresh account before any shortcut has been added.
